Scene(
  objects = {
    table_1 = SceneObject(
      extent = (0.576648, 0.576648, 0.589116),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    )
  },
  num_edits = 1,
  prompt = 'Build a semi-ring structure that gracefully surrounds the central table, providing an inviting layout conducive to gathering and interaction.',
  description = 'This scene involves constructing a semi-ring structure around a centrally positioned table to create a functional and visually interesting setting. The semi-ring should serve as a focal point while providing definition and spatial arrangement within the area. The design must consider geometric properties to ensure the semi-ring integrates well with the table.',
  initial_design = Design(
    structures = {
      circular_structure_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.5,
        inner_radius = 1.0,
        orientation = 'convex',
        height = 0.5
      )
    }
  ),
  design_goal = Design(
    structures = {
      semiring_structure_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.5,
        inner_radius = 1.0,
        orientation = 'convex',
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'circular_structure_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.5,
          inner_radius = 1.0,
          orientation = 'convex',
          height = 0.5
        ),
        edit_description = 'Change the design from a simple circular structure to a SemiRing with an increased outer radius and height to create an open and inviting design.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The edit involves changing the type and parameters of the structure based on clear design goal specifications.'
      )
    ]
  )
)