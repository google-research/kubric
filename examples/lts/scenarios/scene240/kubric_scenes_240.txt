Scene(
  objects = {
    trashcan_1 = SceneObject(
      extent = (0.265243, 0.26524849, 0.3365115),
      center = (-1.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'trashcan'
    ),
    trashcan_2 = SceneObject(
      extent = (0.265243, 0.26524849, 0.3365115),
      center = (1.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'trashcan'
    )
  },
  num_edits = 1,
  prompt = 'Design an artistic path that gracefully connects the two practical disposals, enhancing the elegance of the garden space.',
  description = 'This scene involves creating a curvy path that weaves elegantly around two trashcans to provide an artistic garden pathway. The design requires understanding spatial relationships and interpreting geometric properties such as amplitude, wavelength, and phase to achieve the design goal.',
  initial_design = Design(
    structures = {
      linear_path = Linear(
        center = (0.0, 0.0, 0.0),
        length = 2.0,
        height = 0.1,
        orientation = 'left-right'
      )
    }
  ),
  design_goal = Design(
    structures = {
      curvy_path = CurvyPath(
        far_curve_leftmost_point = (-1.5, -0.2, 0.0),
        close_curve_leftmost_point = (1.5, -0.2, 0.0),
        amplitude = 0.4,
        wavelength = 3.0,
        phase = 0.0,
        height = 0.1
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_path',
        NewDesignStructure = CurvyPath(
          far_curve_leftmost_point = (-1.5, -0.2, 0.0),
          close_curve_leftmost_point = (1.5, -0.2, 0.0),
          amplitude = 0.4,
          wavelength = 3.0,
          phase = 0.0,
          height = 0.1
        ),
        edit_description = 'Change the path profile from linear to CurvyPath to introduce a more dynamic and artistic flow.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'Changing a linear design to a curvy path involves understanding sinusoidal parameters and their effects on path aesthetics, which may not be immediately clear from a straightforward prompt.'
      )
    ]
  )
)