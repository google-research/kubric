Scene(
  objects = {
    rock_1 = SceneObject(
      extent = (0.25, 0.25, 0.25),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'rock'
    )
  },
  num_edits = 2,
  prompt = 'Design a semi-circular ring that elegantly frames the rock, highlighting its natural beauty while ensuring accessibility from all sides.',
  description = 'This scene involves constructing a semi-circular ring around a rock to create a visually pleasing and balanced layout. The SemiRing should enhance the natural features of the scene while maintaining an open and inviting space.',
  initial_design = Design(
    structures = {
      semi_ring_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.0,
        inner_radius = 0.5,
        orientation = 'concave',
        height = 0.3
      )
    }
  ),
  design_goal = Design(
    structures = {
      semi_ring_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.5,
        inner_radius = 1.0,
        orientation = 'convex',
        height = 0.75
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'semi_ring_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.5,
          inner_radius = 1.0,
          orientation = 'concave',
          height = 0.3
        ),
        edit_description = 'Increase both the outer radius to 1.5 meters and the inner radius to 1 meter for a broader enclosure.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The prompt clearly indicates the need for a broader enclosure around the rock.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'semi_ring_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.5,
          inner_radius = 1.0,
          orientation = 'convex',
          height = 0.75
        ),
        edit_description = 'Change the orientation from concave to convex to ensure the open side faces away from the rock, optimizing accessibility and aesthetic appeal.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The orientation change is directly aligned with the design goal of having an open and inviting space.'
      )
    ]
  )
)