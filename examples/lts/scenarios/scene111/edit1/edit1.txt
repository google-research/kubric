Scene(
  objects = {
    table_1 = SceneObject(
      extent = (0.288324, 0.288324, 0.294558),
      center = (1.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    ),
    bench_1 = SceneObject(
      extent = (0.22015424, 0.66046348, 0.28620075),
      center = (0.0, 1.0, 0.0),
      theta = -90.0,
      semantic_label = 'bench'
    )
  },
  num_edits = 2,
  prompt = 'Create a serene seating environment with a semi-circular design that opens towards the table, welcoming guests to sit and relax.',
  description = 'This scene involves constructing a semi-circular seating arrangement around a table and bench, creating a cozy nook in a garden setting. The task requires understanding geometric properties such as radius, height, and orientation to achieve the design goal of a harmonious seating area that encourages relaxation. The focus is on correctly orienting the structure to maintain accessibility and comfort.',
  initial_design = Design(
    structures = {
      SemiCircularSeating = SemiCircular(
        center = (1.0, 1.0, 0.0),
        radius = 1.5,
        orientation = 'concave',
        height = 0.75
      )
    }
  ),
  design_goal = Design(
    structures = {
      SemiCircularSeating = SemiCircular(
        center = (1.0, 1.0, 0.0),
        radius = 1.5,
        orientation = 'concave',
        height = 0.75
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'SemiCircularSeating',
        NewDesignStructure = SemiCircular(
          center = (1.0, 1.0, 0.0),
          radius = 2.0,
          orientation = 'convex',
          height = 0.5
        ),
        edit_description = 'Move the semi-circular seating center to (1, 1) to align appropriately with the table and bench and ensure the structure opens towards the seating area.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The prompt explicitly states the new center (1, 1) for the seating arrangement.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'SemiCircularSeating',
        NewDesignStructure = SemiCircular(
          center = (1.0, 1.0, 0.0),
          radius = 1.5,
          orientation = 'concave',
          height = 0.75
        ),
        edit_description = 'Adjust the radius to 1.5 meters and height to 0.75 meters for a more comfortable seating setup.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = "Altering the radius and height requires geometric understanding but follows the prompt's specifications."
      )
    ]
  )
)