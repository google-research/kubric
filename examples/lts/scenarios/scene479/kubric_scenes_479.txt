Scene(
  objects = {
    birdhouse_1 = SceneObject(
      extent = (0.11663825, 0.16098425, 0.15423275),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'birdhouse'
    )
  },
  num_edits = 2,
  prompt = "Create an artistic enclosure around my feathered friend's home to complement the space while keeping it open and accessible.",
  description = "In this scene, a TriWedge structure is positioned around a birdhouse to create an aesthetically pleasing and defined garden area. The focus is on enhancing the birdhouse's presence while maintaining an open view and access.",
  initial_design = Design(
    structures = {
      Circular_1 = Circular(
        center = (0.0, 0.0, 0.0),
        radius = 0.75,
        height = 0.25
      )
    }
  ),
  design_goal = Design(
    structures = {
      TriWedge_1 = TriWedge(
        center = (0.0, 0.0, 0.0),
        radius = 1.0,
        gap_degrees = 95.0,
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'Circular_1',
        NewDesignStructure = TriWedge(
          center = (0.0, 0.0, 0.0),
          radius = 1.0,
          gap_degrees = 95.0,
          height = 0.5
        ),
        edit_description = 'Change the design from a circular to a TriWedge to add dynamic features and adjust its parameters.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'The prompt suggests an open and accessible design, implying the use of a shape with gaps like the TriWedge. However, the exact dimensions require some interpretation.'
      ),
      1 : DeleteStructureEditWithDifficulty(
        design_id = 'Circular_1',
        edit_description = 'Remove the initial circular structure to transition to the new TriWedge design.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'Removing the structure is straightforward since its purpose is replaced by the more detailed TriWedge design.'
      )
    ]
  )
)