Scene(
  objects = {
    birdhouse_1 = SceneObject(
      extent = (0.5, 0.5, 0.5),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'birdhouse'
    )
  },
  num_edits = 3,
  prompt = 'Provide something crescent-shaped around the birdhouse to protect it from the wind',
  description = 'Scene 4',
  initial_design = Design(
    structures = {
      structure_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.25,
        inner_radius = 1.0,
        orientation = 'concave',
        height = 0.75
      )
    }
  ),
  design_goal = Design(
    structures = {
      goal_structure_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.25,
        inner_radius = 1.0,
        orientation = 'concave',
        height = 0.75
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'structure_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.25,
          inner_radius = 1.0,
          orientation = 'convex',
          height = 0.3
        ),
        edit_description = 'Change the profile to semi-ring',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'The prompt specifies a crescent shape; switching to a semi-ring for protection involves understanding this profile better meets the design goal.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'structure_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.25,
          inner_radius = 1.0,
          orientation = 'convex',
          height = 0.75
        ),
        edit_description = 'Increase the height from 0.3 to 0.75 meters',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = "The height increase is explicitly stated in the 'edits' instructions."
      ),
      2 : ModifyStructureEditWithDifficulty(
        design_id = 'structure_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.25,
          inner_radius = 1.0,
          orientation = 'concave',
          height = 0.75
        ),
        edit_description = 'Change the orientation to top_half to match the wind direction',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'Changing the orientation is prompted by the need to match wind protection, directly aligning with the edits instructions.'
      )
    ]
  )
)