Scene(
  objects = {
    rock_1 = SceneObject(
      extent = (0.1, 0.1, 0.1),
      center = (0.0, -1.0, 0.0),
      theta = 0.0,
      semantic_label = 'rock'
    ),
    rock_2 = SceneObject(
      extent = (0.1, 0.1, 0.1),
      center = (0.0, 1.0, 0.0),
      theta = 0.0,
      semantic_label = 'rock'
    )
  },
  num_edits = 3,
  prompt = 'Craft a meandering path between the rocking stones, just so it gives a delightful twist to the layout.',
  description = 'This scene involves arranging two rocks in a pattern that aligns with a serpentine pathway to create an intriguing aesthetic. The scene requires understanding spatial relationships, such as the orientation and positioning of serpentine curves relative to the rocks. The design also requires precise adjustments to ensure the serpentine does not interfere with the rocks.',
  initial_design = Design(
    structures = {
      serpentine_path = Serpentine(
        left_arc_center = (0.0, 0.0, 0.0),
        radius = 0.5,
        left_arc_orientation = 'up',
        height = 0.15
      )
    }
  ),
  design_goal = Design(
    structures = {
      serpentine_path = Serpentine(
        left_arc_center = (0.0, 0.0, 0.0),
        radius = 0.5,
        left_arc_orientation = 'up',
        height = 0.15
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_path',
        NewDesignStructure = Serpentine(
          left_arc_center = (0.0, 0.0, 0.0),
          radius = 0.5,
          left_arc_orientation = 'up',
          height = 0.15
        ),
        edit_description = 'Change the shape to serpentine',
        difficult_to_deduce_from_prompt = 'medium',
        exaplanation_of_diffuculty = 'The transformation from a linear to a serpentine path requires understanding of curves and geometry.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'serpentine_path',
        NewDesignStructure = Serpentine(
          left_arc_center = (0.0, 0.0, 0.0),
          radius = 0.5,
          left_arc_orientation = 'up',
          height = 0.15
        ),
        edit_description = "Adjust the orientation to 'up' to stay between the rocks",
        difficult_to_deduce_from_prompt = 'easy',
        exaplanation_of_diffuculty = 'Adjusting orientation is straightforward due to clear spatial instructions.'
      ),
      2 : ModifyStructureEditWithDifficulty(
        design_id = 'serpentine_path',
        NewDesignStructure = Serpentine(
          left_arc_center = (0.0, 0.0, 0.0),
          radius = 0.5,
          left_arc_orientation = 'up',
          height = 0.15
        ),
        edit_description = 'Increase the height to 0.15 for a more prominent appearance',
        difficult_to_deduce_from_prompt = 'easy',
        exaplanation_of_diffuculty = 'Increasing height is a simple edit based on visual prominence.'
      )
    ]
  )
)