Scene(
  objects = {
    table = SceneObject(
      extent = (1.5, 1.5, 0.75),
      center = (0.0, 0.0, 0.375),
      theta = 0.0,
      semantic_label = 'table'
    ),
    trashcan = SceneObject(
      extent = (0.5, 0.5, 1.0),
      center = (2.0, 2.0, 0.5),
      theta = 0.0,
      semantic_label = 'trashcan'
    )
  },
  num_edits = 1,
  prompt = 'Create a barrier that separates the trashcan from the table area without blocking too much open space.',
  description = 'This scene involves positioning an L-shaped barrier to create a separation between a table and a trashcan. It requires understanding geometric properties such as center, length, width, and height to achieve the design goal. Additionally, the task involves making decisions regarding the orientation and dimensions of the L-shape to ensure it effectively divides the space while maintaining accessibility.',
  initial_design = Design(
    structures = {
      LShape1 = LShape(
        left_center = (1.0, 1.0, 0.0),
        left_length = 0.5,
        left_width = 0.5,
        right_width = 0.5,
        right_length = 1.0,
        height = 0.2
      )
    }
  ),
  design_goal = Design(
    structures = {
      LShape1 = LShape(
        left_center = (1.25, 1.0, 0.0),
        left_length = 0.2,
        left_width = 2.0,
        right_width = 0.2,
        right_length = 2.0,
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'LShape1',
        NewDesignStructure = LShape(
          left_center = (1.25, 1.0, 0.0),
          left_length = 0.2,
          left_width = 2.0,
          right_width = 0.2,
          right_length = 2.0,
          height = 0.5
        ),
        edit_description = 'Moved and resized the L-shape to better separate the space between the table and trashcan as per the design goal.',
        difficult_to_deduce_from_prompt = 'medium',
        exaplanation_of_diffuculty = 'The edit is of medium difficulty because it involves understanding how to position and size the barrier to achieve the separation without blocking access.'
      )
    ]
  )
)