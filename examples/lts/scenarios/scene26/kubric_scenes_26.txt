Scene(
  objects = {
    table_1 = SceneObject(
      extent = (1.0, 1.0, 0.8),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    ),
    trashcan_1 = SceneObject(
      extent = (0.5, 0.5, 0.8),
      center = (1.5, 1.5, 0.0),
      theta = 0.0,
      semantic_label = 'trashcan'
    )
  },
  num_edits = 2,
  prompt = 'Create an L-shaped barrier around the seating area near the table while maintaining access to the trashcan.',
  description = 'This scene involves understanding spatial relationships, such as positioning an L-shaped structure to demarcate a seating area next to a trashcan. It requires interpreting geometric properties like center, length, width, and height to meet the design goal. The task also involves resolving ambiguities in the instructions, such as determining the correct alignment and dimensions of the L-shape while ensuring the trashcan remains accessible.',
  initial_design = Design(
    structures = {
      linear_1 = Linear(
        center = (0.5, -1.0, 0.0),
        length = 1.0,
        height = 0.3,
        orientation = 'y-axis'
      )
    }
  ),
  design_goal = Design(
    structures = {
      l_shape_1 = LShape(
        left_center = (0.5, 0.0, 0.0),
        left_length = 1.0,
        left_width = 0.2,
        right_center = (0.5, 0.5, 0.0),
        right_length = 0.2,
        height = 0.3
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_1',
        NewDesignStructure = LShape(
          left_center = (0.5, 0.0, 0.0),
          left_length = 1.0,
          left_width = 0.2,
          right_center = (0.5, 0.5, 0.0),
          right_length = 0.2,
          height = 0.3
        ),
        edit_description = 'Change the design profile from linear to L-shape.',
        difficult_to_deduce_from_prompt = 'medium',
        exaplanation_of_diffuculty = 'The prompt suggests creating an L-shape but requires interpretation of how to align and size both segments of the L-shape.'
      ),
      1 : AddStructureEditWithDifficulty(
        design = Design(
          structures = {
            l_shape_1 = LShape(
              left_center = (0.5, 0.0, 0.0),
              left_length = 1.0,
              left_width = 0.2,
              right_center = (0.5, 0.5, 0.0),
              right_length = 0.2,
              height = 0.3
            )
          }
        ),
        edit_description = 'Add the right segment of the L-shape centered at (0.5, 0.5) with a length of 0.2 meters and align with the left segment.',
        target_parameter = 'right segment',
        difficult_to_deduce_from_prompt = 'hard',
        exaplanation_of_diffuculty = 'Aligning the right segment with appropriate length and keeping it consistent with the seating area boundary is challenging.'
      )
    ]
  )
)