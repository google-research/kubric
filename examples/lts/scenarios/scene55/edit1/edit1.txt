Scene(
  objects = {
    pot_1 = SceneObject(
      extent = (0.640426, 0.64042598, 0.432404),
      center = (1.5, -0.5, 0.0),
      theta = 0.0,
      semantic_label = 'pot'
    ),
    trashcan_1 = SceneObject(
      extent = (0.530486, 0.53049698, 0.673023),
      center = (-1.5, 0.5, 0.0),
      theta = 0.0,
      semantic_label = 'trashcan'
    )
  },
  num_edits = 2,
  prompt = 'Create a garden design with a flowing pathway to bridge the two storage units.',
  description = 'This scene involves constructing a wave-like pattern using a curved path and placing it near objects. It requires understanding spatial relationships and geometric properties to create a coherent aesthetic. Additionally, resolving ambiguities in the prompt by determining the correct positioning of the structures is necessary to fulfill the intended design.',
  initial_design = Design(
    structures = {
      curvy_path_1 = CurvyPath(
        far_curve_leftmost_point = (0.0, -1.0, 0.0),
        close_curve_leftmost_point = (0.5, -1.0, 0.0),
        amplitude = 0.2,
        wavelength = 2.5,
        phase = 0.0,
        height = 0.1
      )
    }
  ),
  design_goal = Design(
    structures = {
      curvy_path_1 = CurvyPath(
        far_curve_leftmost_point = (0.0, -1.0, 0.0),
        close_curve_leftmost_point = (0.5, -1.0, 0.0),
        amplitude = 0.2,
        wavelength = 2.5,
        phase = 0.0,
        height = 0.1
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_1',
        NewDesignStructure = CurvyPath(
          far_curve_leftmost_point = (0.0, -1.0, 0.0),
          close_curve_leftmost_point = (0.5, -1.0, 0.0),
          amplitude = 0.15,
          wavelength = 2.0,
          phase = 0.0,
          height = 0.1
        ),
        edit_description = 'Change the profile to a curvy path with specified dimensions.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'The change involves understanding the spatial requirement to replace a linear path with a curvy one, capturing the essence of the design intent.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'curvy_path_1',
        NewDesignStructure = CurvyPath(
          far_curve_leftmost_point = (0.0, -1.0, 0.0),
          close_curve_leftmost_point = (0.5, -1.0, 0.0),
          amplitude = 0.2,
          wavelength = 2.5,
          phase = 0.0,
          height = 0.1
        ),
        edit_description = 'Adjust the amplitude and wavelength to reflect a smoother flow.',
        difficult_to_deduce_from_prompt = 'hard',
        explanation_of_difficulty = 'The adjustment requires precise understanding of how changes to amplitude and wavelength affect the curvy design to achieve a smoother visual flow.'
      )
    ]
  )
)