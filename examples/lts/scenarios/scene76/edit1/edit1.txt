Scene(
  objects = {
    birdhouse_1 = SceneObject(
      extent = (0.2332765, 0.3219685, 0.3084655),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'birdhouse'
    )
  },
  num_edits = 2,
  prompt = 'Create a winding path that leads to the birdhouse, giving it a welcoming entrance.',
  description = "This scene involves creating a winding path that leads to a birdhouse, enhancing the garden's aesthetic and providing a visual guide. It requires understanding geometric properties such as coordinates, amplitude, and wavelength. The task includes resolving spatial relationships and ensuring the path harmonizes with the birdhouse placement.",
  initial_design = Design(
    structures = {
      CurvyPath_1 = CurvyPath(
        far_curve_leftmost_point = (-1.0, -1.0, 0.0),
        close_curve_leftmost_point = (0.5, -0.5, 0.0),
        amplitude = 0.2,
        wavelength = 2.0,
        phase = 0.5,
        height = 0.1
      )
    }
  ),
  design_goal = Design(
    structures = {
      CurvyPath_1 = CurvyPath(
        far_curve_leftmost_point = (-1.0, -1.0, 0.0),
        close_curve_leftmost_point = (0.5, -0.5, 0.0),
        amplitude = 0.2,
        wavelength = 2.0,
        phase = 0.5,
        height = 0.1
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'Linear_1',
        NewDesignStructure = CurvyPath(
          far_curve_leftmost_point = (-1.0, -1.0, 0.0),
          close_curve_leftmost_point = (0.5, -0.5, 0.0),
          amplitude = 0.15,
          wavelength = 2.0,
          phase = 0.4,
          height = 0.1
        ),
        edit_description = "Change the path profile to a curvy path to create a more dynamic entryway. Adjust the path's properties to ensure it complements the birdhouse and fits within the specified area.",
        difficult_to_deduce_from_prompt = 'medium',
        exaplanation_of_diffuculty = 'Requires understanding of geometric transformation and new path creation.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'CurvyPath_1',
        NewDesignStructure = CurvyPath(
          far_curve_leftmost_point = (-1.0, -1.0, 0.0),
          close_curve_leftmost_point = (0.5, -0.5, 0.0),
          amplitude = 0.2,
          wavelength = 2.0,
          phase = 0.5,
          height = 0.1
        ),
        edit_description = 'Adjust the amplitude and phase for a more gentle and inviting curve.',
        difficult_to_deduce_from_prompt = 'medium',
        exaplanation_of_diffuculty = "Fine-tuning the path's properties requires an understanding of impact on visual appearance."
      )
    ]
  )
)