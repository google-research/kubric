Scene(
  objects = {
    tree_1 = SceneObject(
      extent = (0.3, 0.3, 4.0),
      center = (-1.0, 0.0, 2.0),
      theta = 0.0,
      semantic_label = 'tree'
    ),
    tree_2 = SceneObject(
      extent = (0.3, 0.3, 4.0),
      center = (1.0, 0.0, 2.0),
      theta = 0.0,
      semantic_label = 'tree'
    ),
    bench_1 = SceneObject(
      extent = (0.66046348, 0.22015424, 0.28620075),
      center = (0.0, 1.0, 0.143100375),
      theta = 0.0,
      semantic_label = 'bench'
    )
  },
  num_edits = 1,
  prompt = 'Develop a curvy path that embellishes the serene area between my trees while encouraging exploration and relaxation next to the bench.',
  description = "This scene involves creating an engaging curvy path through a garden, ideally framing two trees for a naturally pleasing environment. The task is to ensure the path weaves gracefully between the trees and the bench, enhancing the garden's visual harmony without colliding with any objects.",
  initial_design = Design(
    structures = {
      linear_path_1 = Linear(
        center = (0.0, 0.5, 0.0),
        length = 3.0,
        height = 0.1,
        orientation = 'front-back'
      )
    }
  ),
  design_goal = Design(
    structures = {
      curvy_path_1 = CurvyPath(
        far_curve_leftmost_point = (-1.5, 0.0, 0.0),
        close_curve_leftmost_point = (1.5, 0.0, 0.0),
        amplitude = 0.3,
        wavelength = 2.0,
        phase = 0.0,
        height = 0.1
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_path_1',
        NewDesignStructure = CurvyPath(
          far_curve_leftmost_point = (-1.5, 0.0, 0.0),
          close_curve_leftmost_point = (1.5, 0.0, 0.0),
          amplitude = 0.3,
          wavelength = 2.0,
          phase = 0.0,
          height = 0.1
        ),
        edit_description = "Convert the linear path into a CurvyPath, introducing gentle waves to reflect the garden's natural elements.",
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The prompt explicitly specifies the transition from a linear to a curvy path, providing clear parameters for the CurvyPath design.'
      )
    ]
  )
)