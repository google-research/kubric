Scene(
  objects = {
    tree_1 = SceneObject(
      extent = (0.15, 0.15, 2.0),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'tree'
    ),
    table_1 = SceneObject(
      extent = (0.288324, 0.288324, 0.294558),
      center = (1.5, 0.0, 0.147279),
      theta = 0.0,
      semantic_label = 'table'
    )
  },
  num_edits = 3,
  prompt = 'Build a semi-ring garden feature around the tree to offer a peaceful area while ensuring accessibility.',
  description = 'This scene involves positioning a SemiRing structure to create a protected area around a natural focal point. The scene should enhance the environment while maintaining accessibility and ensuring there are no collisions with the objects in question.',
  initial_design = Design(
    structures = {
      circular_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.8,
        inner_radius = 1.3,
        orientation = 'convex',
        height = 0.5
      )
    }
  ),
  design_goal = Design(
    structures = {
      semi_ring_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.8,
        inner_radius = 1.3,
        orientation = 'convex',
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'circular_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.5,
          inner_radius = 0.0,
          orientation = 'convex',
          height = 0.3
        ),
        edit_description = 'Change the profile from a Circular structure to a SemiRing for nuanced design.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'The edit requires changing the structure type while maintaining initial dimensions.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'circular_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.8,
          inner_radius = 1.3,
          orientation = 'convex',
          height = 0.3
        ),
        edit_description = 'Specify the outer radius as 1.8 and inner radius as 1.3 to ensure a protective yet accessible ring.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'Adjusting the radii involves understanding the protective design goal.'
      ),
      2 : ModifyStructureEditWithDifficulty(
        design_id = 'circular_1',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.8,
          inner_radius = 1.3,
          orientation = 'convex',
          height = 0.5
        ),
        edit_description = 'Adjust the height to 0.5 meters for visual impact and convenience.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'Changing height is a straightforward edit to enhance visual impact.'
      )
    ]
  )
)