Scene(
  objects = {
    rock_1 = SceneObject(
      extent = (0.25, 0.25, 0.25),
      center = (-1.0, 0.0, 0.125),
      theta = 0.0,
      semantic_label = 'rock'
    ),
    rock_2 = SceneObject(
      extent = (0.25, 0.25, 0.25),
      center = (1.0, 0.0, 0.125),
      theta = 0.0,
      semantic_label = 'rock'
    )
  },
  num_edits = 1,
  prompt = 'Create a semi-ring enclosure around each rock, highlighting their natural appeal while ensuring clear paths for viewing and interaction.',
  description = 'This scene involves constructing a `SemiRing` structure around a pair of rocks to create an aesthetic and functional garden enclosure. The goal is to emphasize the presence of the rocks through geometric design while maintaining openness and avoiding overlap.',
  initial_design = Design(
    structures = {
      SemiCircular_1 = SemiRing(
        center = (-1.0, 0.0, 0.0),
        outer_radius = 1.0,
        inner_radius = 0.8,
        orientation = 'convex',
        height = 0.5
      ),
      SemiCircular_2 = SemiCircular(
        center = (1.0, 0.0, 0.0),
        radius = 0.5,
        orientation = 'convex',
        height = 0.2
      )
    }
  ),
  design_goal = Design(
    structures = {
      SemiRing_1 = SemiRing(
        center = (-1.0, 0.0, 0.0),
        outer_radius = 1.0,
        inner_radius = 0.8,
        orientation = 'convex',
        height = 0.5
      ),
      SemiRing_2 = SemiRing(
        center = (1.0, 0.0, 0.0),
        outer_radius = 1.0,
        inner_radius = 0.8,
        orientation = 'convex',
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'SemiCircular_1',
        NewDesignStructure = SemiRing(
          center = (-1.0, 0.0, 0.0),
          outer_radius = 1.0,
          inner_radius = 0.8,
          orientation = 'convex',
          height = 0.5
        ),
        edit_description = 'Change the design from `SemiCircular` to `SemiRing` to add dimension and depth around rock 1, increase the outer radius to 1.0 meters, adjust the inner radius to 0.8 meters, and increase the height to 0.5 meters.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = "The edit involves multiple changes to dimensions and the type of structure, which requires understanding the prompt's goal for dimension and emphasis."
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'SemiCircular_2',
        NewDesignStructure = SemiRing(
          center = (1.0, 0.0, 0.0),
          outer_radius = 1.0,
          inner_radius = 0.8,
          orientation = 'convex',
          height = 0.5
        ),
        edit_description = 'Change the design from `SemiCircular` to `SemiRing` to add dimension and depth around rock 2, increase the outer radius to 1.0 meters, adjust the inner radius to 0.8 meters, and increase the height to 0.5 meters.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = "The edit involves modifying the structure's physical attributes and type, requiring interpretation of the scene's visual and spatial requirements from the prompt."
      )
    ]
  )
)