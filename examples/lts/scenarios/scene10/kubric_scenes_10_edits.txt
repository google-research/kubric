CounterFactualMCQASequence(
  mcqa_questions = [
    0 : EditMCQA(
      question = 'What is the correct edit to add a larger semi-ring structure around the first one?',
      answers = {
        A = AddStructureEdit(
          design = Design(
            structures = {
              semi_ring_2 = SemiRing(
                center = (0.0, 0.0, 0.0),
                outer_radius = 3.5,
                inner_radius = 3.0,
                orientation = 'top_half',
                height = 0.3
              )
            }
          ),
          edit_description = 'Add another larger semi-ring structure around the first one with outer_radius: 3.5 and inner_radius: 3.',
          target_parameter = 'outer_radius'
        ),
        B = AddStructureEdit(
          design = Design(
            structures = {
              semi_ring_2 = SemiRing(
                center = (0.0, 0.0, 0.0),
                outer_radius = 2.0,
                inner_radius = 1.5,
                orientation = 'top_half',
                height = 0.3
              )
            }
          ),
          edit_description = 'Add a smaller semi-ring structure around the first one with outer_radius: 2.0 and inner_radius: 1.5.',
          target_parameter = 'outer_radius'
        ),
        C = AddStructureEdit(
          design = Design(
            structures = {
              semi_ring_2 = SemiRing(
                center = (1.0, 1.0, 0.0),
                outer_radius = 3.5,
                inner_radius = 3.0,
                orientation = 'bottom_half',
                height = 0.3
              )
            }
          ),
          edit_description = "Add another larager semi-ring structure around the first one with a different orientation 'bottom_half'.",
          target_parameter = 'outer_radius'
        ),
        D = AddStructureEdit(
          design = Design(
            structures = {
              semi_ring_2 = SemiRing(
                center = (0.0, 0.0, 0.0),
                outer_radius = 3.5,
                inner_radius = 1.0,
                orientation = 'top_half',
                height = 0.3
              )
            }
          ),
          edit_description = 'Add a larger semi-ring structure around the first one with a larger gap; inner_radius: 1.0.',
          target_parameter = 'outer_radius'
        ),
        E = AddStructureEdit(
          design = Design(
            structures = {
              semi_ring_2 = SemiRing(
                center = (0.0, 0.0, 0.0),
                outer_radius = 2.5,
                inner_radius = 2.0,
                orientation = 'bottom_half',
                height = 0.3
              )
            }
          ),
          edit_description = "Add another identical semi-ring structure like the first one, but with orientation 'bottom_half'.",
          target_parameter = 'outer_radius'
        )
      },
      correct_answer = 'A',
      explanation_of_correctness = 'The correct edit instruction matches the design goal specified, which is to add a semi-ring structure concentrically outside the initial semi-ring.',
      difficulty = 'easy',
      explanation_of_difficulty = 'The prompt explicitly states to create a layered pattern with additional structures, making the correct addition straightforward to deduce.'
    )
  ]
)