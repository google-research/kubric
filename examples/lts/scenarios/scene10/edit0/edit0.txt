Scene(
  objects = {
    rock_1 = SceneObject(
      extent = (0.25, 0.25, 0.25),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'rock'
    )
  },
  num_edits = 1,
  prompt = 'Design a layered landscape pattern around the rock with uniform gaps.',
  description = 'This scene involves arranging multiple semi-ring structures for aesthetic landscaping around a central rock. It requires understanding geometric properties like radius, orientation, and spacing to create a concentric design. The task also includes resolving ambiguities in positioning to maintain uniform gaps between the structures.',
  initial_design = Design(
    structures = {
      semi_ring_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 2.5,
        inner_radius = 2.0,
        orientation = 'top_half',
        height = 0.3
      ),
      semi_ring_2 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 3.5,
        inner_radius = 3.0,
        orientation = 'top_half',
        height = 0.3
      )
    }
  ),
  design_goal = Design(
    structures = {
      semi_ring_1 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 2.5,
        inner_radius = 2.0,
        orientation = 'top_half',
        height = 0.3
      ),
      semi_ring_2 = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 3.5,
        inner_radius = 3.0,
        orientation = 'top_half',
        height = 0.3
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : AddStructureEditWithDifficulty(
        design = Design(
          structures = {
            semi_ring_2 = SemiRing(
              center = (0.0, 0.0, 0.0),
              outer_radius = 3.5,
              inner_radius = 3.0,
              orientation = 'top_half',
              height = 0.3
            )
          }
        ),
        edit_description = 'Add another larger semi-ring structure around the first one with outer_radius: 3.5 and inner_radius: 3.',
        target_parameter = 'outer_radius',
        difficult_to_deduce_from_prompt = 'easy',
        exaplanation_of_diffuculty = 'The prompt explicitly states to create a layered pattern with additional structures, making the required addition straightforward.'
      )
    ]
  )
)