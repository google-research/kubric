Scene(
  objects = {
    table_1 = SceneObject(
      extent = (0.576648, 0.576648, 0.589116),
      center = (0.0, -0.6, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    ),
    table_2 = SceneObject(
      extent = (0.576648, 0.576648, 0.589116),
      center = (0.0, 0.6, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    )
  },
  num_edits = 2,
  prompt = 'Create a semi-ring structure that elegantly frames the tables, offering a subtle division while maintaining openness.',
  description = 'This scene involves constructing a SemiRing structure that provides both functional separation and an aesthetic feature around two tables. The SemiRing structure should be designed to create a sense of enclosure, emphasizing the objects as focal points without obstructing movement.',
  initial_design = Design(
    structures = {
      semi_ring = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.2,
        inner_radius = 0.9,
        orientation = 'concave',
        height = 0.3
      )
    }
  ),
  design_goal = Design(
    structures = {
      semi_ring = SemiRing(
        center = (0.0, 0.0, 0.0),
        outer_radius = 1.5,
        inner_radius = 1.0,
        orientation = 'convex',
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'semi_ring',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.2,
          inner_radius = 0.9,
          orientation = 'convex',
          height = 0.3
        ),
        edit_description = 'Adjust the SemiRing Orientation: Change from concave to convex to open the space towards the tables.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The prompt directly suggests creating openness towards the tables, indicating the need for a convex orientation.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'semi_ring',
        NewDesignStructure = SemiRing(
          center = (0.0, 0.0, 0.0),
          outer_radius = 1.5,
          inner_radius = 1.0,
          orientation = 'convex',
          height = 0.5
        ),
        edit_description = 'Expand the SemiRing Dimensions: Increase the outer radius to 1.5 and inner radius to 1.0 to enlarge the surrounding area and enhance visibility.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'While the prompt mentions maintaining openness and emphasizing the tables, deducing the exact dimensions requires interpreting these requirements spatially.'
      )
    ]
  )
)