Scene(
  objects = {
    table_1 = SceneObject(
      extent = (0.576648, 0.576648, 0.589116),
      center = (0.5, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    ),
    trashcan_1 = SceneObject(
      extent = (0.530496, 0.53049698, 0.673023),
      center = (-0.5, 1.5, 0.0),
      theta = 0.0,
      semantic_label = 'trashcan'
    ),
    pot_1 = SceneObject(
      extent = (0.640426, 0.64042598, 0.432404),
      center = (1.0, -1.0, 0.0),
      theta = 0.0,
      semantic_label = 'pot'
    )
  },
  num_edits = 4,
  prompt = 'Create a linear arrangement to demarcate the dining space, allowing an open flow on one side for easy access.',
  description = 'This scene involves configuring a linear structure to establish a boundary for a gathering space. The design aims to facilitate the functional separation while maintaining open access to the surrounding area. Key considerations include aligning the linear design with spatial features of existing objects, ensuring no collision and respecting the defined area constraints.',
  initial_design = Design(
    structures = {
      linear_1 = Linear(
        center = (0.0, 1.0, 0.0),
        length = 2.0,
        height = 0.8,
        orientation = 'left-right'
      )
    }
  ),
  design_goal = Design(
    structures = {
      linear_2 = Linear(
        center = (0.0, 0.5, 0.0),
        length = 2.0,
        height = 0.8,
        orientation = 'left-right'
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_1',
        NewDesignStructure = Linear(
          center = (0.0, 1.0, 0.0),
          length = 1.5,
          height = 0.5,
          orientation = 'left-right'
        ),
        edit_description = 'Change the orientation from front-back to left-right to establish clear boundaries.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The prompt specifies a linear arrangement that demarcates the dining space with an open flow on one side, suggesting a left-right orientation.'
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_1',
        NewDesignStructure = Linear(
          center = (0.0, 1.0, 0.0),
          length = 2.0,
          height = 0.5,
          orientation = 'left-right'
        ),
        edit_description = 'Adjust the length to 2 meters to match the required span, ensuring coverage between the objects.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'The prompt does not specify the exact length but implies covering a span that requires increasing the design length.'
      ),
      2 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_1',
        NewDesignStructure = Linear(
          center = (0.0, 0.5, 0.0),
          length = 2.0,
          height = 0.5,
          orientation = 'left-right'
        ),
        edit_description = 'Move the center to (0, 0.5, 0) to align with the middle section of the setup, ensuring no conflict with table or trashcan positioning.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'Aligning with existing objects while maintaining access is a non-trivial spatial reasoning task.'
      ),
      3 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_1',
        NewDesignStructure = Linear(
          center = (0.0, 0.5, 0.0),
          length = 2.0,
          height = 0.8,
          orientation = 'left-right'
        ),
        edit_description = 'Increase the height to 0.8 meters for enhanced definition without obstructing the view.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'The height adjustment is inferred to enhance definition while maintaining practicality, requiring some interpretation of visual harmony.'
      )
    ]
  )
)