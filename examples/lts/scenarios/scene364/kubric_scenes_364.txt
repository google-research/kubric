Scene(
  objects = {
    tree_1 = SceneObject(
      extent = (0.15, 0.15, 2.0),
      center = (-1.0, -1.0, 0.0),
      theta = 0.0,
      semantic_label = 'tree'
    ),
    tree_2 = SceneObject(
      extent = (0.15, 0.15, 2.0),
      center = (1.0, 1.0, 0.0),
      theta = 0.0,
      semantic_label = 'tree'
    ),
    bench_1 = SceneObject(
      extent = (0.66046348, 0.22015424, 0.28620075),
      center = (0.0, 0.0, 0.0),
      theta = -90.0,
      semantic_label = 'bench'
    )
  },
  num_edits = 2,
  prompt = '\nDesign a gentle curvy path through the garden that encourages a contemplative stroll between the grand trees \nand the cozy bench.\n',
  description = '\nThis scene involves creating a gracefully curving path that weaves through a small garden area containing two \ntrees and a bench. The path is designed to add aesthetic value and direct movement through the garden, \nencouraging meandering exploration while maintaining harmony with the natural elements.\n',
  initial_design = Design(
    structures = {
      path_1 = Linear(
        center = (0.0, 0.0, 0.0),
        length = 2.0,
        height = 0.2,
        orientation = 'left-right'
      )
    }
  ),
  design_goal = Design(
    structures = {
      path_1 = CurvyPath(
        far_curve_leftmost_point = (-1.5, 0.0, 0.0),
        close_curve_leftmost_point = (1.5, 0.0, 0.0),
        amplitude = 0.3,
        wavelength = 2.0,
        phase = 0.0,
        height = 0.2
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'path_1',
        NewDesignStructure = CurvyPath(
          far_curve_leftmost_point = (-1.5, 0.0, 0.0),
          close_curve_leftmost_point = (1.5, 0.0, 0.0),
          amplitude = 0.3,
          wavelength = 2.0,
          phase = 0.0,
          height = 0.2
        ),
        edit_description = 'Change the design profile to a CurvyPath to introduce a dynamic, smooth flow.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The prompt clearly dictates the transition from a straight path to a curvy one, which aligns with the initial and goal design.'
      ),
      1 : AddStructureEditWithDifficulty(
        design = Design(
          structures = {
            path_1 = CurvyPath(
              far_curve_leftmost_point = (-1.5, 0.0, 0.0),
              close_curve_leftmost_point = (1.5, 0.0, 0.0),
              amplitude = 0.3,
              wavelength = 2.0,
              phase = 0.0,
              height = 0.2
            )
          }
        ),
        edit_description = 'Adjusts far and close leftmost points to create a balanced path weaving between the trees and bench.',
        target_parameter = 'far_curve_leftmost_point, close_curve_leftmost_point',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'Deciding the specific points for the curves required understanding the scene spatial layout which might not be directly derivable from the text alone.'
      )
    ]
  )
)