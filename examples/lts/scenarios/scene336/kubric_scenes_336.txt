Scene(
  objects = {
    birdhouse_1 = SceneObject(
      extent = (0.11663825, 0.16098425, 0.15423275),
      center = (-1.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'birdhouse'
    ),
    trashcan_1 = SceneObject(
      extent = (0.265243, 0.26524849, 0.3365115),
      center = (1.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'trashcan'
    )
  },
  num_edits = 1,
  prompt = 'Create a flowing path that dances between the decorated space, unifying the features without obstructing access.',
  description = 'This scene involves constructing a serpentine path that flows elegantly between a birdhouse and a trashcan. The design requires understanding spatial relationships to ensure the serpentine path decorates the central area without colliding with the objects, maintaining a cohesive layout within the given space constraints.',
  initial_design = Design(
    structures = {
      linear_1 = Linear(
        center = (0.0, 0.0, 0.0),
        length = 2.0,
        height = 0.2,
        orientation = 'left-right'
      )
    }
  ),
  design_goal = Design(
    structures = {
      serpentine_1 = Serpentine(
        left_arc_center = (0.0, 0.0, 0.0),
        radius = 0.75,
        left_arc_orientation = 'convex',
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'linear_1',
        NewDesignStructure = Serpentine(
          left_arc_center = (0.0, 0.0, 0.0),
          radius = 0.75,
          left_arc_orientation = 'convex',
          height = 0.5
        ),
        edit_description = 'Change the path profile from linear to serpentine to introduce dynamic curves and visual interest.',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'The conversion from a linear path to a serpentine path requires repositioning elements and understanding the geometric relationship to achieve the desired flow, which is not explicitly detailed in the prompt.'
      )
    ]
  )
)