Scene(
  objects = {
    table_1 = SceneObject(
      extent = (0.288324, 0.288324, 0.294558),
      center = (1.0, 1.0, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    ),
    table_2 = SceneObject(
      extent = (0.288324, 0.288324, 0.294558),
      center = (-1.0, -1.0, 0.0),
      theta = 0.0,
      semantic_label = 'table'
    )
  },
  num_edits = 2,
  prompt = '\n\n- Design semi-circular sunshades for each table to provide shade without obstructing the view.\n\n',
  description = '\n\nThis scene involves creating a shaded seating area for two tables using semi-circular structures for sun protection. The design requires understanding geometric properties such as radius, orientation, and height to achieve a harmonious layout. The task also involves ensuring no collisions with the tables while maintaining aesthetic and functional goals.\n\n',
  initial_design = Design(
    structures = {
      semi_circular_1 = SemiCircular(
        center = (0.0, 0.0, 0.0),
        radius = 1.5,
        orientation = 'convex',
        height = 0.5
      )
    }
  ),
  design_goal = Design(
    structures = {
      semi_circular_1 = SemiCircular(
        center = (1.0, 1.0, 0.0),
        radius = 0.8,
        orientation = 'convex',
        height = 0.7
      ),
      semi_circular_2 = SemiCircular(
        center = (-1.0, -1.0, 0.0),
        radius = 0.9,
        orientation = 'convex',
        height = 0.7
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : DeleteStructureEditWithDifficulty(
        design_id = 'semi_circular_1',
        edit_description = 'Delete the initial single semi-circular structure as we are splitting it into two separate structures for each table.',
        difficult_to_deduce_from_prompt = 'easy',
        explanation_of_difficulty = 'The need to remove the initial design structure is straightforward since it does not map directly to the design goal of two separate structures.'
      ),
      1 : AddStructureEditWithDifficulty(
        design = Design(
          structures = {
            semi_circular_1 = SemiCircular(
              center = (1.0, 1.0, 0.0),
              radius = 0.8,
              orientation = 'convex',
              height = 0.7
            ),
            semi_circular_2 = SemiCircular(
              center = (-1.0, -1.0, 0.0),
              radius = 0.9,
              orientation = 'convex',
              height = 0.7
            )
          }
        ),
        edit_description = 'Add two semi-circular structures centered at each table with specified radii and heights for aesthetic and functional sunshade coverage.',
        target_parameter = 'structures',
        difficult_to_deduce_from_prompt = 'medium',
        explanation_of_difficulty = 'While the prompt provides the overall idea, detailing the parameters for each new structure requires close attention to the design goal and spatial understanding.'
      )
    ]
  )
)