CounterFactualMCQASequence(
  mcqa_questions = [
    0 : EditMCQA(
      question = 'Hey, try moving that straight line so it stretches more between those two trees! Make it look seamless horizontally, okay?',
      answers = {
        A = ModifyStructureEdit(
          design_id = 'linear_1',
          NewDesignStructure = Linear(
            center = (0.0, 0.0, 0.0),
            length = 1.5,
            height = 0.5,
            orientation = 'left-right'
          ),
          edit_description = 'Relocate the linear path to align along the horizontal axis between the trees. Change the orientation from front-back to left-right, making it parallel between the trees. Extend the length to between the trees and make it taller for a more distinct and engaging boundary.'
        ),
        B = ModifyStructureEdit(
          design_id = 'linear_1',
          NewDesignStructure = Linear(
            center = (0.5, 1.0, 0.0),
            length = 1.0,
            height = 0.3,
            orientation = 'front-back'
          ),
          edit_description = 'Keep the linear path as originally positioned, running front-back near the forward tree. Shorter in length and low on height.'
        ),
        C = ModifyStructureEdit(
          design_id = 'linear_1',
          NewDesignStructure = Linear(
            center = (1.0, 1.0, 0.0),
            length = 2.0,
            height = 0.4,
            orientation = 'left-right'
          ),
          edit_description = 'Position the linear path further right, extend its length significantly, and orient it horizontally with a moderate height.'
        ),
        D = ModifyStructureEdit(
          design_id = 'linear_1',
          NewDesignStructure = Linear(
            center = (-0.5, -0.5, 0.0),
            length = 1.5,
            height = 0.5,
            orientation = 'left-right'
          ),
          edit_description = 'Shift the linear path to the left and lower down to make it blend with the pathway between the trees. The length and height are more pronounced than before.'
        ),
        E = ModifyStructureEdit(
          design_id = 'linear_1',
          NewDesignStructure = Linear(
            center = (0.0, 0.0, 0.0),
            length = 2.5,
            height = 0.7,
            orientation = 'front-back'
          ),
          edit_description = 'Change the linear path to a much longer design with a towering height, maintaining the current center but reorient it along the original direction.'
        )
      },
      correct_answer = 'A',
      explanation_of_correctness = 'The correct modification aligns the linear path along the horizontal axis between the trees, which aligns with the design goal and adjusts both length and height as specified.',
      difficulty = 'easy',
      explanation_of_difficulty = 'The prompt clearly specifies the required changes in position, orientation, and dimensions for the pathway between the trees.'
    )
  ]
)