Scene(
  objects = {
    tree_0 = SceneObject(
      extent = (0.15, 0.15, 2.0),
      center = (0.0, 0.0, 0.0),
      theta = 0.0,
      semantic_label = 'tree'
    )
  },
  num_edits = 2,
  prompt = 'Build a rectangular brick planter next to the tree',
  description = "This scene involves spatial reasoning, where the VLM has to understand the relative positioning of objects like 'next to the tree' and 'in front of the tree.'. Additionally, the instruction requires interpreting geometric transformations, such as swapping dimensions and repositioning the rectangle.",
  initial_design = Design(
    structures = {
      rect_0 = Rectangular(
        center = (2.0, 0.0, 0.0),
        length = 0.5,
        width = 1.0,
        height = 0.5
      )
    }
  ),
  design_goal = Design(
    structures = {
      rect_0 = Rectangular(
        center = (2.0, 0.0, 0.0),
        length = 0.5,
        width = 1.0,
        height = 0.5
      )
    }
  ),
  edits = EditSequence(
    edits = [
      0 : ModifyStructureEditWithDifficulty(
        design_id = 'rect_0',
        NewDesignStructure = Rectangular(
          center = (2.0, 0.0, 0.0),
          length = 1.0,
          width = 0.5,
          height = 0.5
        ),
        edit_description = 'Move the rectangle in front of the tree.',
        difficult_to_deduce_from_prompt = 'medium',
        exaplanation_of_diffuculty = "Determining the position 'in front of the tree' requires understanding relative positioning in a 3D space."
      ),
      1 : ModifyStructureEditWithDifficulty(
        design_id = 'rect_0',
        NewDesignStructure = Rectangular(
          center = (2.0, 0.0, 0.0),
          length = 0.5,
          width = 1.0,
          height = 0.5
        ),
        edit_description = 'Swap the length and width to make the short end face camera horizontally.',
        difficult_to_deduce_from_prompt = 'hard',
        exaplanation_of_diffuculty = 'Swapping dimensions involves both geometric understanding and interpretation of the prompt regarding orientation.'
      )
    ]
  )
)