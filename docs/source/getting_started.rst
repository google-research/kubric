Getting Started
===============

Kubric depends on the Blender python module ``bpy``, which is non-trivial to install.
The easiest way to use kubric is therefore with the provided docker image ``klausgreff/kubruntu:latest``.
